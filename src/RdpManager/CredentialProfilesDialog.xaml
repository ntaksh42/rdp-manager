<Window x:Class="RdpManager.CredentialProfilesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="資格情報プロファイル管理" Height="380" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" UseLayoutRounding="True">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="ProfileList" Grid.Row="0" Grid.Column="0" Margin="0,0,10,0"
                 DisplayMemberPath="Name" SelectionChanged="OnSelect"/>

        <StackPanel Grid.Row="0" Grid.Column="1">
            <TextBlock Text="プロファイル名" Margin="0,0,0,2"/>
            <TextBox x:Name="NameBox" Padding="4,3" Margin="0,0,0,8"/>
            <TextBlock Text="ドメイン" Margin="0,0,0,2"/>
            <TextBox x:Name="DomainBox" Padding="4,3" Margin="0,0,0,8"/>
            <TextBlock Text="ユーザー名" Margin="0,0,0,2"/>
            <TextBox x:Name="UserBox" Padding="4,3" Margin="0,0,0,8"/>
            <TextBlock Text="パスワード" Margin="0,0,0,2"/>
            <PasswordBox x:Name="PassBox" Padding="4,3" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="新規" Width="70" Padding="0,4" Margin="0,0,6,0" Click="OnNew"/>
                <Button Content="保存" Width="70" Padding="0,4" Margin="0,0,6,0" Click="OnSaveProfile"/>
                <Button Content="削除" Width="70" Padding="0,4" Click="OnDelete"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="閉じる" Width="90" Padding="0,4" IsCancel="True" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
