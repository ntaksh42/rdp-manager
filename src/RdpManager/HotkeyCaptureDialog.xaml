<Window x:Class="RdpManager.HotkeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Quick Switch Hotkey" SizeToContent="Height" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" UseLayoutRounding="True"
        Background="{DynamicResource WindowBg}"
        TextElement.Foreground="{DynamicResource TextFg}"
        PreviewKeyDown="OnPreviewKeyDown">
    <StackPanel Margin="14">
        <TextBlock Text="Press the new hotkey combination…" Margin="0,0,0,10" TextWrapping="Wrap"/>
        <Border Background="{DynamicResource PanelBg}" BorderBrush="{DynamicResource ControlBorder}"
                BorderThickness="1" Padding="8" Margin="0,0,0,10">
            <TextBlock x:Name="PreviewText" Text="…" HorizontalAlignment="Center" FontSize="14" FontWeight="Bold"/>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="90" Padding="0,4" Margin="0,0,8,0"
                    IsDefault="True" IsEnabled="False" Click="OnOk"/>
            <Button Content="Cancel" Width="90" Padding="0,4" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
